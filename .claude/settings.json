{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write|MultiEdit",
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.tool_input.file_path // empty' | { read -r f; cd /home/maru/work/me && pnpm biome check --write \"$f\"; } 2>/dev/null || true",
						"statusMessage": "Biome formatting..."
					}
				]
			}
		]
	}
}
